#ifndef WVODH_HH_YAR
#define WVODH_HH_YAR

#include "wvstring.h"

// This is used in various WvTests to count the number of files with
// a certain prefix in a directory.
int wvtest_file_count_prefix(WvStringParm dirname, WvStringParm prefix);

#endif

